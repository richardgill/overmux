# @overmux/shared

Internal contracts and platform conventions shared by independently built Overmux components.

The environment-neutral root contains shared schemas. Node-specific filesystem conventions are available from `@overmux/shared/node`. User-facing APIs remain exposed by their owning packages.
