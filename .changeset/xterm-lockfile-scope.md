---
---

The shared lockfile changes only install the xterm patch, browser-test tooling, and tmux's raw-xterm addon test dependency (plus pnpm peer-reference normalization). The xterm and tmux releases are covered separately; no independent releases are needed for the other packages flagged by the shared-lockfile check.
